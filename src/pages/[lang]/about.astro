---
import Base from '../../layouts/Base.astro';
import { SUPPORTED, t, DEFAULT_LANG, type Lang } from '../../i18n';

export function getStaticPaths() {
  return SUPPORTED.map((lang) => ({ params: { lang } }));
}
const lang = (Astro.params.lang as Lang | undefined) ?? DEFAULT_LANG;
---
<Base title={`Auris — ${t(lang, 'nav_about')}`} lang={lang}>
  <section class="mx-auto max-w-[1200px] px-4 py-10">
    <h1 class="text-2xl font-bold">{t(lang, 'about_title')}</h1>
    <p class="mt-3 text-white/80">{t(lang, 'about_intro')}</p>
  </section>
</Base>